
Class mainFuncion {
    insert () {
        If (index == 0) 
           { n.setNext(root.getNext());}
        Else 
           { insert(index - 1, root.getNext(), n)[Math.ceil(10, 5)[iphone.get()]] = 1; }
    }
}