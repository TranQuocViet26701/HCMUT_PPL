
Class makeSchool {
        secondary(a : Int) {
            io.fflush(stdin);
            io.getline(lass);
            If (lass != "-1") 
                {School.updateClass(lass);}
            Else { Break;}
        }
    setNext(next : Node) {
        Self.next = next;
    }
}